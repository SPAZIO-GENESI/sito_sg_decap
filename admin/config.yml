backend:
  name: github
  repo: spazio-genesi/sito_sg_decap
  branch: main

  base_url: https://decap-oauth-proxy.it-e3f.workers.dev
  auth_endpoint: /auth
  client_id: Ov23liHhBJ2OIvs4V06t

media_folder: "static/uploads"
public_folder: "/static/uploads"

collections:
  - name: "pages"
    label: "Pagine"
    folder: "content/pages"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Titolo", name: "title", widget: "string" }
      - { label: "Corpo", name: "body", widget: "markdown" }
